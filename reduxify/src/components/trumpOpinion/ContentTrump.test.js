import React from 'react';
import { shallow, mount } from 'enzyme';

import ContentTrump from './ContentTrump';
import { Provider } from 'react-redux';
import store from '../../store/index';

describe('<ContentTrump /> ', () => {
  // const contentWrapper = shallow(<ContentTrump />);
  /*
    ============
    OUTPUT TESTS
    ============
  */
  // it('renders correctly', () => {
  //  expect(contentWrapper).toHaveLength(1);
  // });
  // it('renders a <div>', () => {
  //   const div = contentWrapper.find(div);
  //   expect(div).toHaveLength(1);
  // });
  //
  // it('<div> renders two <div>', () => {
  //   const div = contentWrapper.find(div).find(div);
  //   expect(div).toHaveLength(2);
  // });




})
